name: settings
description: "Módulo de configuración y backup para MyLifeOS."
version: 0.0.1

resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  file_picker: ^8.3.4
  shared_preferences: ^2.5.4
  core:
    path: ../../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
